# npm project settings for FihDar.
# legacy-peer-deps / shamefully-hoist were pnpm-era leftovers and are removed:
# under npm they suppressed required peer dependencies (e.g. webpack, required
# by @sentry/webpack-plugin), which broke `npm ci` in production builds.
