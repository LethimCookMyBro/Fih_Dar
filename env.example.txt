# =================================================================
# FihDar — environment variables (names only, never commit real values)
# Copy to `.env` for local development.
# =================================================================

# -----------------------------------------------------------------
# Database — PostgreSQL (Railway "Postgres" plugin provides this)
# -----------------------------------------------------------------

DATABASE_URL=

# -----------------------------------------------------------------
# Report image storage
# -----------------------------------------------------------------
# Absolute path the server writes uploaded report images to.
# Railway: mount a Volume and point this at its mount path,
#   e.g. /data/fihdar/uploads
# Local: leave empty to use ./.data/uploads (git-ignored).

UPLOAD_DIR=

# -----------------------------------------------------------------
# Map basemap
# -----------------------------------------------------------------
# Optional. Any MapLibre-compatible vector style URL.
# Defaults to OpenFreeMap Liberty (OpenStreetMap data, no API key).
# Public by design — this is the only NEXT_PUBLIC_ map variable.

NEXT_PUBLIC_MAP_STYLE_URL=

# -----------------------------------------------------------------
# Authentication (Clerk)
# -----------------------------------------------------------------
# Leave both empty to use Clerk keyless mode in development.

NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# -----------------------------------------------------------------
# Field operations (officer authorization)
# -----------------------------------------------------------------
# Comma-separated Clerk user ids allowed to record field-operation results
# on citizen reports. The allowlist is enforced server-side — leaving this
# empty means NO account can perform officer actions.

OFFICER_CLERK_USER_IDS=

NEXT_PUBLIC_CLERK_SIGN_IN_URL="/auth/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/auth/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/map"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/map"

# Only needed if Clerk webhooks are wired up.
WEBHOOK_SECRET=

# -----------------------------------------------------------------
# Build / deployment
# -----------------------------------------------------------------
# Public origin of the deployed app, used for metadata absolute URLs.
NEXT_PUBLIC_APP_URL=

# Set to "true" for Docker / self-hosted standalone output.
BUILD_STANDALONE=

# -----------------------------------------------------------------
# Error tracking (Sentry) — optional
# -----------------------------------------------------------------

NEXT_PUBLIC_SENTRY_DSN=
NEXT_PUBLIC_SENTRY_ORG=
NEXT_PUBLIC_SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
NEXT_PUBLIC_SENTRY_DISABLED="false"
